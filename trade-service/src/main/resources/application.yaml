server:
  port: 8085
feign:
  okhttp:
    enabled: true # 开启OKHttp功能
#  sentinel:
#    enabled: true # 开启feign对sentinel的支持

hm:
  swagger:
    title: 商品服务接口文档
    package: com.hmall.trade.controller
  db:
    database: hm-trade